import { StubPage } from "@/components/_dev/StubPage";

export default function Page() {
  return (
    <StubPage
      title="Onboarding 2/4 — name"
      phase="Phase 3"
      meta="Single text field → babies.name"
    />
  );
}
