import { StubPage } from "@/components/_dev/StubPage";

export default function Page() {
  return (
    <StubPage
      title="Onboarding 3/4 — feeding"
      phase="Phase 3"
      meta="Multi-select: breast / formula / mixed / solids → babies.feeding_type"
    />
  );
}
