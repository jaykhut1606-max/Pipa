import { StubPage } from "@/components/_dev/StubPage";

export default function Page() {
  return (
    <StubPage
      title="Onboarding 4/4 — concerns"
      phase="Phase 3"
      meta="Multi-select; final step inserts babies row + sets profiles.onboarded_at."
    />
  );
}
