import { StubPage } from "@/components/_dev/StubPage";

export const metadata = { title: "Chat with Pippa" };

export default function Page() {
  return (
    <StubPage
      title="Chat with Pippa"
      phase="Phase 7"
      meta="AI pediatric chat with streaming response. System prompt per spec Part 6.5."
    />
  );
}
