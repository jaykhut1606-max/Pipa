import { StubPage } from "@/components/_dev/StubPage";

export const metadata = { title: "Scan" };

export default function Page() {
  return (
    <StubPage
      title="Scan center"
      phase="Phase 6"
      meta="Three cards: Diaper, Cry, Rash + a link to AI chat."
    />
  );
}
