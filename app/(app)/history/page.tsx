import { StubPage } from "@/components/_dev/StubPage";

export const metadata = { title: "History" };

export default function Page() {
  return (
    <StubPage
      title="History"
      phase="Phase 6"
      meta="Last 30 days of scans + tracker events merged into one timeline."
    />
  );
}
