import { StubPage } from "@/components/_dev/StubPage";

export const metadata = { title: "Privacy" };

export default function Page() {
  return (
    <StubPage
      title="Privacy"
      phase="Phase 9"
      meta="Termly-based template, customized for photo handling. Photos and audio are never stored — only structured AI output."
    />
  );
}
